require "./spec_helper"

describe Decidim::Cli do
  context "should display" do
    it "ruby version" do
      fail "not implemented ruby version"
    end

    it "decidim version" do
      fail "not implemented decidim version"
    end

    it "gems list with path and branchs" do
      fail "not implemented gems list with path and branchs"
    end

    it "current git branch" do
      fail "not implemented current git branch"
    end

    it "forge name for the current decidim gem" do
      fail "not implemented forge name"
    end
  end
end
